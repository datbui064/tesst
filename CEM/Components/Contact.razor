@page "/contact"
<!DOCTYPE html>
<html lang="vi">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Liên Hệ</title>
    <style>
        body {
            font-family: 'Arial', sans-serif;
            background: linear-gradient(to right, #e9ecef, #ffffff);
         
            perspective: 1200px; /* Tạo hiệu ứng 3D mạnh hơn */
        }

        h1 {
            text-align: center;
            color: #007BFF;
            margin: 20px ;
            text-shadow: 2px 2px 4px rgba(0, 0, 0, 0.3);
        }

        .container {
            display: flex;
            justify-content: space-between;
            align-items: flex-start;
            gap: 30px;

            transform: rotateY(5deg); /* Tạo hiệu ứng 3D */
        }

        .form-container {
            flex: 1;
            background: white;
            padding: 20px;
            border-radius: 12px;
            box-shadow: 0 8px 30px rgba(0, 0, 0, 0.3);
            transition: transform 0.3s, box-shadow 0.3s;
        }

            .form-container:hover {
                transform: translateY(-15px) scale(1.03);
                box-shadow: 0 12px 50px rgba(0, 0, 0, 0.4);
            }

        label {
            display: block;
            margin: 10px 0 5px;
            font-weight: bold;
            color: #495057;
        }

        input[type="text"],
        input[type="email"],
        textarea {
            width: 100%;
            padding: 12px;
            margin-bottom: 15px;
            border: 2px solid #007BFF;
            border-radius: 6px;
            transition: border-color 0.3s, box-shadow 0.3s;
        }

            input[type="text"]:focus,
            input[type="email"]:focus,
            textarea:focus {
                border-color: #0056b3;
                box-shadow: 0 0 10px rgba(0, 86, 179, 0.5);
                outline: none;
            }

        input[type="submit"] {
            background-color: #007BFF;
            color: white;
            padding: 12px 15px;
            border: none;
            border-radius: 6px;
            cursor: pointer;
            transition: background-color 0.3s, transform 0.3s;
        }

            input[type="submit"]:hover {
                background-color: #0056b3;
                transform: scale(1.05);
                box-shadow: 0 4px 20px rgba(0, 0, 0, 0.2);
            }

        .map-container {
            flex: 1;
            border-radius: 12px;
            overflow: hidden;
            box-shadow: 0 8px 30px rgba(0, 0, 0, 0.3);
            transition: transform 0.3s;
        }

        iframe {
            width: 100%;
            height: 300px;
            border: none;
            transition: transform 0.3s;
        }

        .map-container:hover iframe {
            transform: scale(1.05) rotateZ(2deg); /* Thêm hiệu ứng xoay nhẹ */
        }

        .location-info {
            margin-top: 20px;
            background: #f8f9fa;
            padding: 15px;
            border-radius: 8px;
            box-shadow: 0 8px 20px rgba(0, 0, 0, 0.2);
        }

            .location-info h2 {
                color: #007BFF;
                margin-bottom: 10px;
            }

            .location-info p {
                color: #343a40;
            }
    </style>
</head>
<body>
    <div>
        <h1>Liên Hệ Chúng Tôi</h1>
        <div class="container">
            <div class="form-container">
                <form action="#" method="post">
                    <label for="name">Họ và Tên:</label>
                    <input type="text" id="name" name="name" required>

                    <label for="email">Email:</label>
                    <input type="email" id="email" name="email" required>

                    <label for="message">Tin Nhắn:</label>
                    <textarea id="message" name="message" rows="5" required></textarea>

                    <input type="submit" value="Gửi">
                </form>
            </div>
            <div class="map-info-container">
                <div class="map-container">
                    <iframe src="https://www.google.com/maps/embed?pb=!1m18!1m12!1m3!1d4438.290162828804!2d105.79007882585606!3d20.98247783936549!2m3!1f0!2f0!3f0!3m2!1i1024!2i768!4f13.1!3m3!1m2!1s0x3135accf04011535%3A0xa93dfa61fb043aaf!2zSOG7jWMgVmnhu4duIEFuIG5pbmggTmjDom4gRMOibg!5e1!3m2!1svi!2s!4v1733847140111!5m2!1svi!2s" loading="lazy" referrerpolicy="no-referrer-when-downgrade"></iframe>
                </div>
                <div class="location-info">
                    <h2>Thông tin liên hệ</h2>
                    <h3>Học viện An ninh</h3>
                    <p>Địa chỉ: 125 Đường Trần Phú, P. Văn Quán, Hà Đông, Hà Nội 100000, Việt Nam</p>
                    <p>Điện thoại: 091 881 0587</p>
                    <p>Email: buiphatdat815@gmail.com</p>
                </div>
            </div>
        </div>
    </div>
</body>
</html>


